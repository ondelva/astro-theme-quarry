dist/

# pnpm writes this; Prettier rewrites every line of it and pnpm writes it back.
pnpm-lock.yaml

# MDX content. Prettier's Markdown printer reflows a fenced code block that sits inside
# a JSX element and returns it as one line, and these files are prose we set line by
# line anyway.
src/content/**/*.mdx
